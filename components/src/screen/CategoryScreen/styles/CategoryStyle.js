import React, { useState, useEffect } from 'react';
import {
    StyleSheet,
} from 'react-native';
const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    imgDish: {
        marginTop: 10,
        width: '100%',
        height: 150,
        resizeMode: 'contain',

    },
    cardProduct: {

    }
})
export default styles